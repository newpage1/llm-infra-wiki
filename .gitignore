# ── 施工快照与中间产物 ────────────────────────────────────────────
# .backup/ 是脚本改写 analyses.js 之前留的整文件快照（一度 50MB）。
# 仓库进入 git 之后，历史由 git 接管，这些快照不再需要。
.backup/

# .work/ 是逐模块施工的中间产物（模块 JSON、脚手架、临时构建脚本，约 8MB）。
# 其中真正值得留下的写作规范已提取到 authoring/，其余是过程垃圾。
.work/

# ── Python ───────────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── 编辑器 / 系统 ────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── 依赖目录（本项目其实不需要，占位防止误提交）──────────────────
node_modules/
.venv/
venv/
