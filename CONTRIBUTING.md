# 参与贡献

**怎么改、怎么跑校验、怎么提 PR，都在 [README](README.md) 里**——那是最新的入口，
不要在本文里找流程，以免两份文档说法不一致。

本文只留一件事：**动手之前必须接受的内容口径**。

---

## 内容口径

这四条是这个站点区别于「又一份翻译稿」的地方。写之前先读一遍。

### 1. 每句话要指得到某一行

论断后面给 `路径:行号`，**行号必须真实存在**。不是"参考了某某文件"，
而是"这一行就是我说的事"。

`tools/verify-anchors.sh` 会逐条核对：行号越界、代码围栏与原文不一致，都会报错。

### 2. 图放上面，说明放下面

每个小节都是「先给总述 → 再放图 → 图下解读」。图的解读要能独立读懂，
不能只写"如上图所示"。

### 3. 诚实标注，不编行号

| 情况 | 怎么写 |
|---|---|
| 没读到 | 「未覆盖」 |
| 靠推理 | 「推断」 |
| 找不到调用方 | 「未发现调用方」（并说明你搜过的方式） |
| 不确定 | 「未确认」 |

**把没锚上的东西写清楚，比编一个行号有价值得多。** 每份子任务汇报里都有一栏
「哪些论断没锚上」——那一栏比改了多少字更重要。

### 4. 钉住版本

所有引用都相对分析页 `revision` 字段里那个提交。上游代码会漂，
漂了就是「详情页路径过期」这类问题——**发现请开
[纠错 Issue](https://github.com/newpage1/llm-infra-wiki/issues/new?template=correction.yml) 或直接改**。

---

## 提 PR 之前确认

1. `bash tools/verify.sh` 全绿（**CI 跑的就是它，红了合不进去**）
2. 改了 `路径:行号` 或代码围栏 → 跑过 `bash tools/verify-anchors.sh <分析页>`
3. 改了 `assets/` 下的静态资源 → 跑过 `python3 bump.py`
4. 新增或改写关键模块 → 读过 [`authoring/MODULE-SPEC.md`](authoring/MODULE-SPEC.md)

各份规范的分工见 README 的「文档在哪」一节。
